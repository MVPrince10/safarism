# safarism — Context

**2026-08-15 (T-2) — SHIPPED.** Redesigned around the real Safari Supermarket brand. Prince approved the before/after screenshots (`output/screenshots/safarism-redesign/`), asked for three copy edits (no hero caption, "grocer", plain sentences instead of dashes), then gave the go. Merged `safarism-redesign` → `main` (fast-forward, `aa73363`), pushed; Amplify job **6** SUCCEED (18:20 MT); live HTML verified via CloudFront: `/brand/mark.png`, `/icon.png`, `/apple-icon.png`, `/opengraph-image.png` all 200 and referenced. Worktree removed, branch deleted.

Left as-is on purpose: `lib/site.ts` `description` still reads "neighborhood grocery … —" (it feeds `<meta description>` / og:description only, not on-page copy). Off-limits for T-2; a one-line edit if Prince wants it aligned. Prince's original logo files were moved to `output/brand-originals/` (gitignored, throwaway) once normalised into `public/brand/`.

What changed:
- Brand assets normalised: `public/brand/mark.png` (transparent mark, 695×430) + `public/brand/lockup.webp` (trimmed full lockup; the file Prince dropped as `.jpeg` was really WebP). Originals removed from `public/photos/`.
- `app/icon.tsx` (letter "S" favicon) replaced by static `app/icon.png` (64), `app/apple-icon.png` (180), `app/opengraph-image.png` (1200×630, lockup on white) — generated by `scripts/brand-assets.mjs`. OG no longer uses `store-1.jpg`.
- Palette in `app/globals.css` derived from the mark (cream / forest / forest-deep / olive / rust / sun), each token commented; old green/bg/card placeholders gone.
- Type: Outfit (display) + Source Sans 3 (body) via `next/font`. Rationale: Outfit's geometric caps sit next to the lockup's wordmark without fighting it; Source Sans 3 is quiet and reads well at hours-table sizes. Wordmark in nav/footer is live HTML type beside the mark (crisper than cropping the lockup at small sizes); the lockup is used where it can be large (OG card).
- Home: masthead (mark + wordmark, Hours/Contact, phone as outlined button) → hero with `store-1` (the real interior) as one strong photo with caption → dark forest "Visit us" band (hours · address · phone/email, amber underline) → editorial "What we carry" (`store-2` tall + text + `store-3`) → closing address line + CTAs → footer with mark. No cards, no pills, no gradients.
- Contact: eyebrow + H1 + intro, form left (square-cornered fields, forest focus), store info stacked right behind a hairline.
- Lint + build exit 0; contrast checked (all text ≥ 4.6:1); images via `next/image` with `sizes` + alt.

Infra state (unchanged from T-1):
- Amplify app `d19tdnpxsxl0d8` (us-west-2, profile personal), auto-deploys `main`; build needs node 24 + linux lockfile (`amplify.yml` pins nvm 24).
- Route53 zone `Z0980712B6Q1FSFIJ4MK`; apex A alias + www CNAME → CloudFront `d15x0oj9nvw39x`; www 301→apex custom rule. Registrar stays Squarespace Domains (nameservers delegated). Cutover completed 2026-08-15; note the Amplify domain association shows the apex `verified: False` even though it resolves and serves 200 — harmless, but a stale local resolver still holding Squarespace answers will see an apex↔www 301 loop until its cache expires.

Open: swap `lib/site.ts` `formspreeEndpoint` to a Safari-specific Formspree form notifying uantoinette@gmail.com (I-7). Currently reuses lmno's form; submissions tagged `_subject: Safari Supermarket website`.
